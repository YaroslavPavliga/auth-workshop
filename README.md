'EOF'
# Практичне завдання: Security & Auth — JWT, OAuth2, Roles

Цей проект демонструє реалізацію безпеки REST API з використанням:
- JWT (JSON Web Tokens) для аутентифікації
- RBAC (Role-Based Access Control) для авторизації
- Основ OAuth2

## 🚀 Швидкий старт

### Встановлення
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
